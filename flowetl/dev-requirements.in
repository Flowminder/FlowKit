# FlowETL additional dev requirements

# Airflow and python versions must match the versions in the base docker image
-c https://raw.githubusercontent.com/apache/airflow/constraints-${AIRFLOW_VERSION}/constraints-${PYTHON_VERSION}.txt
-c requirements.txt

## 'file:' required here to prevent compiling to absolute path (https://github.com/jazzband/pip-tools/issues/204#issuecomment-785420275)
#-e file:./flowetl
structlog
pytest
psycopg2-binary
pandas
docker
versioneer
pytest-cov